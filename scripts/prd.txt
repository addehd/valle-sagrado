This project lets users create profile pages.

Each profile page should show a list of services.

Each service has:
- title
- description
- image

I want to:
1. Create a Supabase `services` table
2. Display services on the profile page
3. Edit services in an `[edit-service].svelte` page
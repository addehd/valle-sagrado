{
    "mcpServers": {
        "task-master-ai": {
            "command": "npx",
            "args": [
                "-y",
                "--package=task-master-ai",
                "task-master-ai"
            ],
            "env": {
                "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}",
                "OPENAI_API_KEY": "${OPENAI_API_KEY}"
            }
        },
        "valle-sagrado": {
            "url": "https://valle-sagrado-eknx5s3id-addehds-projects.vercel.app/api/mcp"
        }
    }
}